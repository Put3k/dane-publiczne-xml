# Public host used to build CSV resource URLs (no trailing slash)
HTTP_HOST=https://localhost

SMTP_HOST=smtp.host
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASSWORD=yourpassword123
SMTP_FROM=whosend
SMTP_TO=recipient@email.com
